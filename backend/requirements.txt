# Target runtime: Python 3.12.x (pinned in .python-version).
# Minimum-version constraints allow pip to use pre-installed packages
# on Python 3.14 during development until Python 3.12 is installed via:
#   py install 3.12

fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.11.0
pydantic-settings>=2.6.1
pyyaml>=6.0.2
pytest>=8.3.0
httpx>=0.27.0
openpyxl>=3.1.0
aiofiles>=24.1.0
gdown>=5.2.0
